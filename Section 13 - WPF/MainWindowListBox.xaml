<UserControl x:Class="Section_13___WPF.MainWindowListBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Section_13___WPF"
        mc:Ignorable="d"
        d:DesignHeight="450" 
        d:DesignWidth="800" 
        >
     
    <Grid ShowGridLines="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions> 
        
        <!-- <ListBox x:Name="ListBoxNames" Grid.Column="1" Grid.Row="1"/> -->
        <ListBox x:Name="ListBoxPeople" SelectionMode = "Multiple" Grid.Column="1" Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Path=Name, StringFormat='Name: {0}'}"/>
                        <TextBlock Text="{Binding Path=Age,  StringFormat='Age: {0}'}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <Button Click = "ButtonBase_OnClick" Grid.Row="3" Grid.Column="1" Content="Show Selected"/>

    </Grid>
</UserControl>